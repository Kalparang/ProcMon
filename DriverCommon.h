#define OBPrefix L"obprefix"
#define FSPrefix L"fsprefix"
#define REGPrefix L"regprefix"

VOID
SetDriverName(
    PWCH NT_NAME,
    PWCH DOS_NAME
);

VOID
SetOBName(
    PWCH NT_NAME,
    PWCH DOS_NAME
);

VOID
SetFSName(
    PWCH NT_NAME,
    PWCH DOS_NAME
);

VOID
SetREGName(
    PWCH NT_NAME,
    PWCH DOS_NAME
);